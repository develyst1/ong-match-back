PORT=3010
DATABASE_URL=postgresql://user:pass@host:5432/ong_match_db
# Optional. `bun test` (NODE_ENV=test) uses this, or falls back to <db>_test.
TEST_DATABASE_URL=postgresql://user:pass@host:5432/ong_match_db_test
AI_BASE_URL=https://ai.develyst.online
# Absolute base used to build uploaded-image URLs. Defaults to http://localhost:PORT.
PUBLIC_BASE_URL=http://localhost:3010
JWT_SECRET=change-me-to-a-long-random-string
